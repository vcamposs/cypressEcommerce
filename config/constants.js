const properties = {
    timeout: {
        url: 30000,
        labelHeader: 10000,
        loadingSearchFlights: 60000,
        genericElement: 5000,
    }
};

module.exports = {
    properties: properties
};